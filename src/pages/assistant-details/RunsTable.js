import React from 'react';
import { Box } from '@mui/material';

function RunsTable() {
    return (
        <Box>
            <h1> Runs Table </h1>
        </Box>
    );
}

export default RunsTable;
